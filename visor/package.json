{
  "name": "visor-arboles",
  "version": "1.0.0",
  "description": "Web visor for detected crop trees — Fontanars dels Alforins",
  "type": "module",
  "scripts": {
    "tiles": "node scripts/build-data.js",
    "predev": "node scripts/build-data.js",
    "dev": "vite",
    "prebuild": "node scripts/build-data.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "maplibre-gl": "^4.3.2"
  },
  "devDependencies": {
    "geojson-vt": "^4.0.2",
    "vite": "^5.4.0",
    "vt-pbf": "^3.1.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
